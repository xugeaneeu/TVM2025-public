{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install deps if needed",
            "type": "shell",
            "command": "node ./.vscode/check-deps.js",
            "problemMatcher": []
        },
        {
            "label": "build",
            "command": "pnpm",
            "args": ["--aggregate-output", "-r", "build"],
//            "options": {"cwd": "${fileDirname}/../"},
            
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "dependsOn": ["Install deps if needed"]
        }
    ]
}