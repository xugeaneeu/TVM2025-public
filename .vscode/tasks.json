{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "pnpm",
            "args": ["-r", "build"],
//            "options": {"cwd": "${fileDirname}/../"},
            
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}