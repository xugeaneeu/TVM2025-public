Arithmetic {
	Expr = AddSub
  AddSub = DivMul (("+" | "-") DivMul)*
  DivMul = Prim (("*" | "/") Prim)*
  Prim = "-" Prim     --unmin
       | number       --num
       | var          --var
       | "(" Expr ")" --par

  var = letter (alnum)*
  number = digit+
}